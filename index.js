(function () {
  'use strict';
  // Shim to avoid "module is not defined" in browser if some legacy code references it
  window.module = window.module || { exports: {} };
  // Add page-specific JS here if needed
})();